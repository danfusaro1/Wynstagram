<%= form_for(@wstagram) do |f| %>
  <% if @wstagram.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wstagram.errors.count, "error") %> prohibited this wstagram from being saved:</h2>

      <ul>
      <% @wstagram.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.text_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
